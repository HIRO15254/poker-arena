# Poker Bot Arena — 開発ルール

仕様の全体は [SPEC.md](SPEC.md) を参照。

## デザインシステム(必須)

- 本プロジェクトの UI は **Cryst Design System** を前提に開発する。
- 色・タイポグラフィ・スペーシング・コンポーネントは必ず Cryst のトークン/コンポーネントから取ること。独自の色やコンポーネントを発明しない。Cryst に無いものが必要な場合は、Cryst の語彙を拡張する形で設計し、その旨をユーザーに明示する。
- Cryst Design System の参照先: Claude Design プロジェクト「Cryst Design System」(projectId: `6c0df305-076f-4746-a326-a80d8b2e731e`、DesignSync ツールで読み取り)。ローカル抜粋は [design-system/cryst/](design-system/cryst/NOTES.md)(トークン CSS + 要点メモ)。

## 画面実装前のデザインレビュー(必須・例外なし)

新しい画面、または既存画面の大きな UI 変更を実装する前に、必ず以下の手順を踏むこと:

1. `/design` スキルでデザインカンバス(モックアップ)を作成する。デザインは Cryst Design System のトークン・コンポーネントに忠実であること。
2. ユーザーにレビューを依頼し、**明示的な承認**を得る。
3. 承認を得てから初めて実装コードを書く。レビュー指摘があればデザインを修正して再レビュー。

- ユーザーの承認なしに画面の UI 実装を始めてはならない。
- 例外: コピー修正・バグ修正・ロジックのみの変更など、見た目に実質的な影響がない変更はレビュー不要。
