# Poker Bot Arena 仕様書 v0.1

各自が開発したポーカーボットを持ち寄って対戦させるアリーナサイト。
AIエージェント(Claude Code 等)による bot 開発を第一級にサポートする。

ステータス: ドラフト(2026-08-20 のヒアリングに基づく)

---

## 1. コンセプト

- ユーザーは NLHE 6-max のポーカーボットを登録し、24/7 稼働の常設リングゲームで他者の bot と対戦させる。
- リーダーボードは bb/100 ベース。
- 人間向け Web UI とは別に、**開発者の AI エージェント向けの REST API + MCP サーバー**を提供し、
  「ハンド履歴を AI に読ませて弱点を直させる」開発ループを回せるようにする。

## 2. ゲーム仕様

| 項目 | 値 |
|---|---|
| ゲーム | ノーリミット・テキサスホールデム |
| テーブル | **種目はシーズンごとに設定。シーズン1はヘッズアップ(HU、2人)**。6-max は後続シーズンで導入予定(エンジンは 2〜9 人対応済み) |
| ブラインド | SB 0.5bb / BB 1bb(内部チップ: SB 50 / BB 100、1bb = 100 チップ) |
| スタック | **毎ハンド 100bb にリセット**(常に 100bb effective。bb/100 の測定を安定させるため) |
| レーキ | ポットの 5%、**キャップ 0.6bb**、no flop no drop(シーズン1=HU の値。6-max 導入時に再検討) |
| アンティ / ストラドル | なし |
| その他 | 標準ルール(min-raise、サイドポット、ショーダウン順序は Robert's Rules 準拠) |

## 3. リーグ・レーティング

- **シーズン制**: リーグは **1ヶ月単位のシーズン**で運行。シーズン終了時にリーダーボードを確定・アーカイブして表彰し、翌シーズンは成績リセットで開始。ゲームルール(§2)はシーズン共通。
  - **シーズン中に bot を再デプロイすると、その bot のシーズン成績はリセット**される(ランキングは常に現行バージョンの成績。バージョン別の内訳は本人のみ閲覧可)。
  - **シーズン1はアップロード型・Webhook 型の両方が揃ってから開幕**(Phase 3 完了後)。それまでは**シーズン0(ベータ)**として Webhook のみで運用し、成績は非公式扱い。
  - シーズン表彰の内容(賞・称号等)は未決(§15)。
- **運行形態**: 24/7 連続リングゲーム。単一プール(レーティング帯による分離はしない)。
- **テーブル編成**: 稼働中 bot からマッチメイカーが 6 席を編成。一定ハンド数(例: 100 ハンド)ごとに席をシャッフルし、相手分布とポジションの偏りを平準化。ボタンは毎ハンド通常ローテーション。
- **同一ユーザーの bot は同卓しない**(コルージョン防止)。
- **レーティング指標**: bb/100(レーキ控除後)。
  - リーダーボード掲載条件: 直近バージョンで最低 10,000 ハンド(暫定値)。**シーズン1(ベータ)は 2,000 ハンド**。
  - 表示: bb/100、ハンド数、95% 信頼区間。
  - bot の新バージョンをデプロイすると成績はバージョン単位で分離(通算表示も可)。
- **他者に公開される情報**: bot 名、所有者、bb/100、ハンド数のみ。VPIP/PFR 等の統計・ハンド履歴は本人のみ(§6)。

## 4. Bot 実行方式(2 方式)

### 4.1 アップロード型(Python サンドボックス)

- 対応言語: **Python 3.12 のみ**。標準ライブラリ + 許可リストのパッケージ(numpy 等。リストは別途確定)。
- **提出は 1 ファイル (.py) のみ。コード長の上限あり**:
  - `#` コメントと空行を除いた UTF-8 バイト数で **8,192 バイト(8KB)以内**
  - ファイル全体の生バイト数は 256KB 以内(コメントによる水増しの保険)
  - **docstring を含む文字列リテラルは数える**。免除するとレンジ表を docstring に置いて
    `__doc__` からパースする抜け道ができるため。長い説明は `#` コメントで書く
  - 狙いは「データ量ではなくロジックで勝負させる」こと。巨大な事前計算テーブルは同梱できない
  - 手元で確認: `pnpm measure <bot.py>` または `python3 sdk/python/check_bot.py <bot.py>`
- Cloudflare Sandbox SDK 上のコンテナで実行。**テーブル着席中のみプロセス起動**(コスト制御)。
- プロセスは着席セッション中は永続(ハンドごとの再起動なし。メモリ内に状態を保持可能)。
- 制約: **ネットワーク egress 禁止**、CPU / メモリ上限あり(具体値は未定)。追加ファイルの同梱は不可。
- インターフェース: stdin/stdout の JSONL。1 行 = 1 ゲームイベント受信、アクション要求行に対して 1 行で応答。

### 4.2 Webhook 型(セルフホスト)— **現在停止中**

> シーズン1のベータ期間中は受け付けない(`SeasonConfig.webhookBotsEnabled: false`)。
> アリーナから外部への通信を一切行わない状態にしてある。再開は同フラグを true にするだけ。

- ユーザーが HTTPS エンドポイントを登録。アリーナがゲーム状態を POST し、レスポンスでアクションを受け取る。
- リクエストには HMAC 署名を付与(bot ごとのシークレット)。
- 言語・計算リソースは自由(外部 solver 等の利用も可。縛りは制限時間のみ)。
- 可用性要件: タイムアウトはタイムバンク制(§5)で処理。エラー/タイムアウトが継続する場合は自動離席。

> 両方式は同一リーグ・同一プロトコルで混在する。

## 5. アクション時間制限(タイムバンク制)

| 項目 | 値(暫定) |
|---|---|
| 基本制限時間 | アップロード型: 2 秒 / Webhook 型: 5 秒(ネットワーク往復を考慮) |
| タイムバンク | 初期 **1 秒**。基本制限を超過した分を消費。ハンド開始ごとに **+0.5 秒**回復(上限 **10 秒**) |
| バンク枯渇時 | check 可能なら check、不可なら fold |
| 自動離席 | 連続 20 アクションがタイムアウト処理された場合。bot は error 状態になり再デプロイまで復帰しない |

## 6. ハンド履歴・情報公開

- ハンド履歴は**自分視点のみ**: 自 bot のホールカードは常に記録、相手のカードはショーダウンで公開されたもののみ。
- マックされたカードは誰にも公開しない。
- ライブ観戦(§8)ではホールカードは一切非表示、結果のみ。
- 履歴フォーマット: 独自 JSON(プロトコルと同型)+ 標準テキスト形式(PokerStars 形式互換)のエクスポート。

## 7. AI 開発支援(REST API + MCP)

### 7.1 認証

- ユーザー単位の API キー(スコープ: 読み取り / bot 管理)。Web UI から発行。

### 7.2 MCP サーバー(リモート MCP、Streamable HTTP)

| ツール | 内容 |
|---|---|
| `get_rules` | ゲームルール・リーグ規定の取得 |
| `get_protocol_spec` | bot プロトコル仕様(JSON Schema 付き)の取得 |
| `list_my_bots` | 自分の bot 一覧と稼働状態 |
| `get_bot_stats` | bb/100 推移、ポジション別成績、基礎統計(VPIP/PFR/3bet 等) |
| `get_hand_histories` | フィルタ付きハンド履歴取得(期間、ポジション、損益順、ポットサイズ等) |
| `upload_bot_version` | 新バージョンのデプロイ(アップロード型) |
| `run_test_match` | 非公式テスト対戦(ベンチマーク bot or 自分の bot 同士、指定ハンド数)。レーティング非反映 |
| `get_leaderboard` | リーダーボード取得 |

- REST API は MCP と同一機能を提供(MCP は REST の薄いラッパー)。
- ドキュメントは機械可読を最優先: `llms.txt`、JSON Schema、OpenAPI。

### 7.3 スターターキット

- Python テンプレート(状態パース、ハンド評価、脚注付きサンプル戦略)。
- **ローカルシミュレータ**(同一ゲームエンジンを npm/pip パッケージ化 or 単体バイナリ): アップロード前に手元で bot 同士を回せる。AI エージェントが最も高速にループを回せる場所。

## 8. Web UI(v1 スコープ)

1. **リーダーボード** — bb/100 ランキング、bot 詳細ページ。
2. **bot 管理画面** — 登録、バージョン管理・デプロイ、稼働状態、エラーログ、API キー発行。
3. **ライブテーブル観戦** — 進行中テーブルを WebSocket でリアルタイム表示(ホールカード非表示)。
4. **ハンドリプレイヤー** — 自 bot の過去ハンドをステップ再生。

※ ブラウザ内コードエディタは v1 スコープ外(アップロードは Web UI のファイルアップロード / API 経由)。

表記ルール(UI):
- 金額は UI 上 **bb 単位**で表示(内部・プロトコルはチップ絶対値のまま)。
- カードは **4色デック**(♠黒 / ♥赤 / ♦青 / ♣緑)。
- テーブル画面ではアクションの**全履歴**をストリート別に常時表示。

## 9. 認証・参加制限

- **GitHub OAuth** でログイン(v1 未実装 — OAuth アプリの登録が必要なため、現在は `POST /api/signup` で
  名前を登録し API キーを受け取る簡易方式。公開前に OAuth へ差し替える)。
- 1 ユーザーあたり bot **3 個**まで(暫定)。全 bot 同時稼働可、ただし同卓しない。
- API レート制限あり。

## 10. アーキテクチャ(Cloudflare)

| コンポーネント | 役割 |
|---|---|
| Workers | REST API / Web 配信(静的アセット)/ cron によるリーグ進行 |
| D1 | ユーザー、bot、シーズン成績、ハンド履歴、プレイセッション |
| Cron Triggers | 毎分リーグバッチを実行(ペアを総当たりで対戦させ成績を更新) |
| Workers Assets | フロントエンド(React SPA) |

**v1 での構成判断**: 当初 Durable Objects + R2 + Queues を想定していたが、無料プランで動かせること・
デプロイ手数を減らすことを優先し、**D1 + cron のみ**で実装した。プレイセッションは
`(デッキシード, hero のアクション列)` から決定的に再構築するため、サーバー側にインメモリ状態を持たない。
真のリアルタイム観戦(WebSocket)と Durable Objects は 6-max 導入時に再検討する。

### リポジトリ構成(TypeScript モノレポ、pnpm workspaces)

| パッケージ | 内容 |
|---|---|
| `packages/engine` | ゲームエンジン(純粋ロジック、DO 非依存、RNG 注入可) |
| `packages/protocol` | ゲーム状態/アクションの型定義 + JSON Schema(bot 開発者・MCP と共有) |
| `packages/simulator` | ローカルシミュレータ CLI(エンジン再利用、内蔵ベンチマーク bot) |
| `apps/api` | Workers + Hono(REST/MCP)、テーブル DO、マッチメイカー DO |
| `apps/web` | React SPA |

- **リポジトリは GitHub public** で開発。サーバー側 RNG のシード等の秘密は環境変数/Secrets 管理とし、コードの公開自体が公平性を損なわない設計を保つ。

### コスト制御

- テーブルのハンド進行ペースをサーバー側で制御(目安: 1 ハンド 10〜30 秒)。
- 同時稼働テーブル数に上限(登録 bot 数に応じて拡張)。
- 非稼働 bot のサンドボックスは即時停止。

## 11. プロトコル概要(ドラフト)

アクション要求(アリーナ → bot):

```json
{
  "type": "act",
  "hand_id": "h_01J8...",
  "seat": 2,
  "hole_cards": ["As", "Kd"],
  "board": ["7h", "2c", "Jd"],
  "pot": 650,
  "players": [
    {"seat": 0, "stack": 9400, "bet": 300, "status": "active"},
    {"seat": 2, "stack": 9700, "bet": 0, "status": "active"}
  ],
  "actions": [
    {"seat": 0, "street": "flop", "action": "bet", "amount": 300}
  ],
  "legal_actions": [
    {"action": "fold"},
    {"action": "call", "amount": 300},
    {"action": "raise", "min": 600, "max": 9700}
  ],
  "time_remaining_ms": 2000,
  "time_bank_ms": 2500
}
```

応答(bot → アリーナ):

```json
{"action": "raise", "amount": 900}
```

- 金額は全てチップ絶対値(1bb = 100)。
- raise の amount は「そのストリートでの合計ベット額(raise to)」。
- 不正なアクションはタイムアウトと同様に check-fold 処理。

## 12. 公平性・セキュリティ

- サンドボックスはネットワーク遮断・リソース制限。コードは実行前に静的チェック(サイズ、禁止 import)。
- Webhook 型は計算リソース無制限を明示的に許容(縛りは時間のみ)。方式間の公平性はルールとして明記。
- RNG: v1 はサーバー側 CSPRNG を信頼。将来的に verifiable shuffle(シードコミット方式)を検討。
- コルージョン対策 v1: 同一ユーザー同卓禁止のみ。チップダンピング検出は将来課題。

## 13. ロードマップ

| Phase | 内容 |
|---|---|
| 1 | ゲームエンジン(TypeScript、DO 非依存の純粋ロジック)+ ローカルシミュレータ + プロトコル確定 + Python スターターキット |
| 2 | DO テーブル + Webhook bot 対応 + GitHub OAuth + 最小 UI(リーダーボード、bot 管理) |
| 3 | サンドボックス実行(アップロード型)+ 24/7 リーグ稼働(タイムバンクは実装済み) |
| 4 | MCP サーバー + ハンド履歴 API + ハンドリプレイヤー |
| 5 | ライブ観戦 + ベンチマーク bot 群 + run_test_match |

## 14. デザインシステム・開発プロセス

- UI は **Cryst Design System** を前提に開発する。色・タイポグラフィ・スペーシング・コンポーネントは Cryst のトークン/コンポーネントに準拠し、独自スタイルを発明しない。
- **画面実装前のデザインレビューを必須とする**: 新規画面・大きな UI 変更は、実装前にデザインカンバス(モックアップ)を作成してユーザーレビューを受け、明示的な承認を得てから実装する。詳細な運用ルールは [CLAUDE.md](CLAUDE.md) に記載。

## 15. 未決事項

- サイト名
- シーズン表彰の内容(賞・称号・アーカイブページの見せ方)
- 許可 Python パッケージのリスト、サンドボックスのリソース上限具体値
- リーダーボード掲載の最低ハンド数、bot 上限数の最終値
- run_test_match の回数制限
- レーキの端数処理(チップ単位切り捨て想定)
- 席シャッフルの間隔とアルゴリズム詳細
